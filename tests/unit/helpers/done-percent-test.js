import { donePercent } from 'capstone-client/helpers/done-percent';
import { module, test } from 'qunit';

module('Unit | Helper | done percent');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = donePercent([42]);
  assert.ok(result);
});
